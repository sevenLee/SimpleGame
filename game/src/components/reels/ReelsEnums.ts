export enum SymbolStatePostfix {
    STATIC = '',
    BLUR = '_Blur',
    LOSE = '_Lose'
}

export enum SymbolState {
    STATIC = 0,
    BLUR = 1,
    LOSE = 2
}

export enum SymbolAnimationPostfix {
    IDLE = '_Idle',
    WIN = '_Win',
    DROPPED = '_Drop',
    ANTICIPATION = '_Anticipation'
}